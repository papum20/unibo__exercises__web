import { useState } from "react";

function App() {
	const [workAvailable, setWorkAvailable] = useState(null);

	return (
		<div>
			{ workAvailable && 
				<
			}
		</div>
	);
}

export default App;
